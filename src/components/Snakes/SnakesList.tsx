import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SnakesList = () => {
  return (
    <View>
      <Text>SnakesList</Text>
    </View>
  )
}

export default SnakesList

const styles = StyleSheet.create({})